type Contract {
    _id: String!
    policyHolderId: [PersonGrainValue]
    insuredPersonId: [PersonGrainValue]
    term: [GrainValue]
    value: [GrainValue]
}

type PersonGrainValue {
    _in_utc: String!
    _v: String!
    _c: Float
}

type GrainValue {
    _in_utc: String!
    _v: String!
    _c: Float
}